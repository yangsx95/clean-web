name: Build

on:
  workflow_dispatch:
    inputs:
      build_windows:
        description: "Build the unsigned Windows installer"
        required: true
        type: choice
        default: "true"
        options:
          - "true"
          - "false"
      build_macos:
        description: "Build the unsigned macOS Universal DMG"
        required: true
        type: choice
        default: "true"
        options:
          - "true"
          - "false"
      build_android:
        description: "Build the unsigned Android package"
        required: true
        type: choice
        default: "false"
        options:
          - "false"
          - "true"
      build_ios:
        description: "Build the unsigned iOS simulator package"
        required: true
        type: choice
        default: "false"
        options:
          - "false"
          - "true"
      publish_release:
        description: "Publish a GitHub Release after building"
        required: true
        type: choice
        default: "false"
        options:
          - "false"
          - "true"
      release_tag:
        description: "Release tag to publish, for example v0.1.0. Required only when publishing."
        required: false
        type: string
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - ".github/workflows/build.yml"
      - ".mise.toml"
      - "package.json"
      - "package-lock.json"
      - "apps/desktop/**"
      - "apps/mobile/**"
      - "crates/**"
      - "packages/frontend/**"
      - "docs/platforms/mobile.md"
      - "docs/platforms/android.md"
      - "docs/platforms/ios.md"
      - "docs/project-structure.md"
  pull_request:
    paths:
      - ".github/workflows/build.yml"
      - ".mise.toml"
      - "package.json"
      - "package-lock.json"
      - "apps/desktop/**"
      - "apps/mobile/**"
      - "crates/**"
      - "packages/frontend/**"
      - "docs/platforms/mobile.md"
      - "docs/platforms/android.md"
      - "docs/platforms/ios.md"
      - "docs/project-structure.md"

permissions:
  contents: read

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect-changes:
    name: Detect changed targets
    runs-on: ubuntu-latest
    outputs:
      desktop: ${{ steps.filter.outputs.desktop }}
      mobile: ${{ steps.filter.outputs.mobile }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Detect desktop and mobile changes
        id: filter
        shell: bash
        run: |
          set -euo pipefail

          desktop=false
          mobile=false

          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            desktop=${{ inputs.build_windows == 'true' || inputs.build_macos == 'true' }}
            mobile=${{ inputs.build_android == 'true' || inputs.build_ios == 'true' }}
          elif [[ "${{ startsWith(github.ref, 'refs/tags/') }}" == "true" ]]; then
            desktop=true
            mobile=true
          else
            if [[ "${{ github.event_name }}" == "pull_request" ]]; then
              base="${{ github.event.pull_request.base.sha }}"
              head="${{ github.event.pull_request.head.sha }}"
            else
              base="${{ github.event.before }}"
              head="${{ github.sha }}"
              if [[ "$base" =~ ^0+$ ]]; then
                base="$(git rev-list --max-parents=0 "$head")"
              fi
            fi

            while IFS= read -r file; do
              case "$file" in
                .github/workflows/build.yml|.mise.toml|package.json|package-lock.json|packages/frontend/**)
                  desktop=true
                  mobile=true
                  ;;
                apps/desktop/**)
                  desktop=true
                  ;;
                crates/**)
                  desktop=true
                  ;;
                apps/mobile/**|docs/platforms/mobile.md|docs/platforms/android.md|docs/platforms/ios.md|docs/project-structure.md)
                  mobile=true
                  ;;
              esac
            done < <(git diff --name-only "$base" "$head")
          fi

          echo "desktop=$desktop" >> "$GITHUB_OUTPUT"
          echo "mobile=$mobile" >> "$GITHUB_OUTPUT"

  static-validation:
    name: Validate static assets
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.desktop == 'true' || needs.detect-changes.outputs.mobile == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Check patch whitespace
        run: git diff --check

      - name: Validate bundled rules
        run: node scripts/rules/validate-clash-rules.mjs

  frontend-validation:
    name: Validate frontend
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.desktop == 'true' || needs.detect-changes.outputs.mobile == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test frontend
        run: npm test

      - name: Build desktop frontend
        if: needs.detect-changes.outputs.desktop == 'true'
        run: npm run build

      - name: Build mobile frontend
        if: needs.detect-changes.outputs.mobile == 'true'
        run: npm run build:mobile

  desktop-rust-validation:
    name: Validate desktop Rust
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.desktop == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust checks
        uses: actions/cache@v5
        with:
          path: ~/.cache/clean-web/mise
          key: rust-checks-${{ runner.os }}-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock', 'apps/desktop/src-tauri/Cargo.toml', 'crates/**/Cargo.toml') }}
          restore-keys: |
            rust-checks-${{ runner.os }}-

      - name: Install dependencies
        run: npm ci

      - name: Install Linux Tauri dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            patchelf \
            pkg-config \
            xdg-utils

      - name: Check Rust formatting
        run: cd apps/desktop/src-tauri && cargo fmt --check

      - name: Test Rust
        run: node scripts/mise/rust-task.mjs test

      - name: Lint Rust
        run: node scripts/mise/rust-task.mjs lint

  build-windows:
    name: Build unsigned Windows installer
    runs-on: windows-latest
    needs:
      - detect-changes
      - static-validation
      - frontend-validation
    if: github.event_name != 'pull_request' && needs.detect-changes.outputs.desktop == 'true' && (github.event_name != 'workflow_dispatch' || inputs.build_windows == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri -> target
          key: desktop-windows
          save-if: false

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri NSIS installer
        run: npm run tauri -- build --bundles nsis

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v7
        with:
          name: cleanweb-windows-unsigned
          path: |
            apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
            apps/desktop/src-tauri/target/release/*.exe
          if-no-files-found: error

  build-macos-universal:
    name: Build unsigned macOS Universal DMG
    runs-on: macos-latest
    needs:
      - detect-changes
      - static-validation
      - frontend-validation
    if: github.event_name != 'pull_request' && needs.detect-changes.outputs.desktop == 'true' && (github.event_name != 'workflow_dispatch' || inputs.build_macos == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Rust targets
        run: rustup target add x86_64-apple-darwin aarch64-apple-darwin

      - name: Cache Rust
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri -> target
          key: desktop-macos
          save-if: false

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri Universal DMG
        run: npm run tauri -- build --target universal-apple-darwin --bundles dmg

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: cleanweb-macos-universal-unsigned
          path: |
            apps/desktop/src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg
            apps/desktop/src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app
          if-no-files-found: error

  build-android:
    name: Build unsigned Android package
    runs-on: ubuntu-latest
    needs:
      - detect-changes
      - static-validation
      - frontend-validation
    if: github.event_name != 'pull_request' && needs.detect-changes.outputs.mobile == 'true' && (github.event_name != 'workflow_dispatch' || inputs.build_android == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Rust targets
        run: rustup target add aarch64-linux-android armv7-linux-androideabi i686-linux-android x86_64-linux-android

      - name: Cache Rust
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/mobile/src-tauri -> target
          key: mobile-android
          save-if: false

      - name: Install dependencies
        run: npm ci

      - name: Build Android APK
        run: npm run tauri:mobile -- android build --apk

      - name: Upload Android artifacts
        uses: actions/upload-artifact@v7
        with:
          name: cleanweb-android-unsigned
          path: |
            apps/mobile/src-tauri/gen/android/app/build/outputs/apk/**/*.apk
            apps/mobile/src-tauri/gen/android/app/build/outputs/bundle/**/*.aab
          if-no-files-found: error

  build-ios:
    name: Build unsigned iOS simulator package
    runs-on: macos-latest
    needs:
      - detect-changes
      - static-validation
      - frontend-validation
    if: github.event_name != 'pull_request' && needs.detect-changes.outputs.mobile == 'true' && (github.event_name != 'workflow_dispatch' || inputs.build_ios == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Rust targets
        run: rustup target add aarch64-apple-ios-sim x86_64-apple-ios

      - name: Cache Rust
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/mobile/src-tauri -> target
          key: mobile-ios
          save-if: false

      - name: Install dependencies
        run: npm ci

      - name: Build unsigned iOS simulator app
        run: npm run tauri:mobile -- ios build --target aarch64-sim --no-sign --ci

      - name: Package iOS artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ios-artifacts
          find apps/mobile/src-tauri/gen/apple -type f -name "*.ipa" -exec cp {} ios-artifacts/ \;
          while IFS= read -r -d '' path; do
            name="$(basename "$path")"
            ditto -c -k --sequesterRsrc --keepParent "$path" "ios-artifacts/${name}.zip"
          done < <(find apps/mobile/src-tauri/gen/apple -type d \( -name "*.app" -o -name "*.xcarchive" \) -print0)
          ls -la ios-artifacts

      - name: Upload iOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: cleanweb-ios-simulator-unsigned
          path: ios-artifacts/*
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs:
      - static-validation
      - frontend-validation
      - desktop-rust-validation
      - build-windows
      - build-macos-universal
      - build-android
      - build-ios
    if: always() && github.event_name == 'workflow_dispatch' && inputs.publish_release == 'true' && needs.static-validation.result == 'success' && needs.frontend-validation.result == 'success' && ((inputs.build_windows != 'true' && inputs.build_macos != 'true') || needs.desktop-rust-validation.result == 'success') && (inputs.build_windows != 'true' || needs.build-windows.result == 'success') && (inputs.build_macos != 'true' || needs.build-macos-universal.result == 'success') && (inputs.build_android != 'true' || needs.build-android.result == 'success') && (inputs.build_ios != 'true' || needs.build-ios.result == 'success')
    permissions:
      contents: write

    steps:
      - name: Validate release tag
        run: |
          if [ "${{ inputs.build_windows }}" != "true" ] && [ "${{ inputs.build_macos }}" != "true" ] && [ "${{ inputs.build_android }}" != "true" ] && [ "${{ inputs.build_ios }}" != "true" ]; then
            echo "At least one build target must be enabled when publishing." >&2
            exit 1
          fi
          if [ -z "${{ inputs.release_tag }}" ]; then
            echo "release_tag is required when publish_release is true" >&2
            exit 1
          fi
          case "${{ inputs.release_tag }}" in
            v*) ;;
            *)
              echo "release_tag must start with v, for example v0.1.0" >&2
              exit 1
              ;;
          esac

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-artifacts

      - name: Publish release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ inputs.release_tag }}
          files: |
            release-artifacts/**/*.dmg
            release-artifacts/**/*.exe
            release-artifacts/**/*.apk
            release-artifacts/**/*.aab
            release-artifacts/**/*.ipa
            release-artifacts/**/*.zip
          generate_release_notes: true
