Tauri v2（`tauri::Builder` + `tauri::generate_handler!` + `tauri::generate_context!`），macOS 通过 `libc::kill` 信号与 `osascript` 管理员 shell 交互，Windows 通过 advapi32/kernel32/shell32 FFI 实现 UAC 提权；SQLite 持久化由 `storage::AppState` 管理。